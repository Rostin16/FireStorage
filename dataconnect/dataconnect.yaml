specVersion: "v1beta"
serviceId: "FireStorage-PR"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "FireStorage-PR-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
